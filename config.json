{
    "templates": {
        "pumps": "validPump({p}, {s}).",
        "components": {
            "modes": {
                "force": "defComp({c}, Lo, Hi, D, {n}, {z}) :- Lo = {lo} / err * err , Hi = (({hi} - 1) / err + 1) * err, D = {hi} - {lo}.",
                "safe":  "defComp({c}, Lo, Hi, D, {n}, {z}) :- Lo = (({lo} - 1) / err + 1) * err , Hi = {hi} / err * err, D = {hi} - {lo}."
            },
            "active": "force"
        }
    },
    "count": -1,
    "classicDisplay": false,
    "weights": {
        "aCov": 1,
        "eCov": 1
    }
}